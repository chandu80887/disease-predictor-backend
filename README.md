# AI-Powered Symptom-Based Disease Prediction System — Backend

## Run locally
pip install -r requirements.txt
python app.py

## Deploy on Render
See deployment guide provided separately. Uses gunicorn (Procfile) and reads PORT from env.
